#!/usr/bin/env groovy

node {
    stage('checkout') {
        deleteDir()
        checkout scm
    }

    stage('prepare pushing to test') {
        fileRenameOperation('.clever-prod.json', '.clever.json')
    }

    stage('Deploy to clever') {
        sh 'clever deploy'
    }
}
